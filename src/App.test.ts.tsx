import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

describe('AccessibilityTools Component', () => {
  it('App: deve renderizar com o tema padrão', () => {});
});
